# Prize-Draw-API
A repository to trasnform my original API written in PHP into C#. The API controls the quantity of prizes, does the caculations for the draw based on a plethora of configurations and saves everything it does and every change. It can easily be used in raffle type of cenários
